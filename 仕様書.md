# Excalidraw フォルダ指定機能開発ガイド - FastAPI + フォーク運用

## プロジェクト概要

### 目標

- URL に保存フォルダパスを含める機能を追加
- 任意のフォルダに Excalidraw ファイルを保存(パスは URL で指定する)
- クロスプラットフォーム対応（Mac M1、Windows、Linux）
- ネットワークフォルダ対応
- **FastAPI バックエンドでファイル管理**
- python は既にインストール済のものを使用する。仮想環境は不要
- **GitHub フォークによる継続的アップデート対応**

### アーキテクチャ

```
┌─────────────────────┐    ┌─────────────────────┐
│   Excalidraw        │────│   FastAPI Backend   │
│   (フロントエンド)    │    │   (ファイル管理)      │
│   - React/TypeScript │    │   - Python 3.8+     │
│   - File System API  │    │   - ファイルI/O       │
│   - カスタムUI        │    │   - フォルダ管理      │
└─────────────────────┘    └─────────────────────┘
```

### Excalidraw 仕様

- **最新バージョン**: 最新の安定版(master ブランチ)を使用する
- **ファイル形式**: JSON（.excalidraw 拡張子）
- **バンドル形式**: ESM（v0.18.0 から UMD から ESM に移行）
- **フロントエンド**: File System Access API + FastAPI 連携
- **バックエンド**: FastAPI（Python）でファイル操作

## claude code

- 開発にあたり、読み込みが不要なファイルはコンテキストに含めないこと
- 開発にあたって、ドキュメントを作成し、ドキュメントも更新していくこと
- 応答やドキュメントは日本語でアウトプットすること

## 開発環境セットアップ

### 1. フォークリポジトリの作成と管理

```bash

# 1. フォークしたリポジトリをクローン
git clone https://github.com/myao9494/excalidraw.git
cd excalidraw

# 2. アップストリーム設定（継続的アップデート用）
git remote add upstream https://github.com/excalidraw/excalidraw.git
git fetch upstream

# 3. 開発ブランチ作成
git checkout -b dev
git push -u origin dev

# 4. フロントエンド依存関係のインストール
yarn install
```

### 2. FastAPI バックエンドセットアップ

```bash
# プロジェクトルートにバックエンド作成
mkdir backend
cd backend

# Python仮想環境作成
python -m venv venv

# 仮想環境有効化
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# FastAPI依存関係インストール
pip install fastapi uvicorn python-multipart aiofiles pathlib

# requirements.txt作成
pip freeze > requirements.txt
```

### 3. プロジェクト構造（開発関連ファイルのみ）

```
excalidraw/
├── src/                     # フロントエンド（編集対象）
│   ├── actions/
│   │   └── actionExport.ts  # 保存機能修正
│   ├── components/
│   │   └── MainMenu.tsx     # UI修正
│   ├── data/
│   │   └── filesystem.ts    # 新規作成
│   └── types.ts             # 型定義追加
├── backend/                 # FastAPIバックエンド（新規作成）
│   ├── main.py              # FastAPIメイン
│   ├── routers/
│   │   └── files.py         # ファイル操作API
│   ├── services/
│   │   └── file_manager.py  # ファイル管理ロジック
│   ├── models/
│   │   └── schemas.py       # Pydanticモデル
│   └── requirements.txt
├── .gitignore               # 開発不要ファイル除外
└── README.md                # プロジェクト説明
```

### 4. 継続的アップデート戦略

```bash
# 定期的にアップストリームから更新を取得
git fetch upstream
git checkout main
git merge upstream/main

# コンフリクト解決後、開発ブランチに反映
git checkout feature/folder-management
git rebase main

# フィーチャーブランチをプッシュ
git push origin feature/folder-management --force-with-lease
```

### Claude Code での自動セットアップ

```bash
# プロジェクト全体セットアップ
claude code "Setup forked Excalidraw repository with FastAPI backend"

# 継続的アップデート設定
claude code "Configure upstream tracking for Excalidraw updates"
```

### 5. オフライン動作の確認

- ネットワークを切断した状態でも動作することを確認
- Service Worker によりリソースがキャッシュされる
- ファイル保存・読み込み機能が正常に動作することを確認

## 参考資料・URL 集

### GitHub リポジトリ

- **メインリポジトリ**: https://github.com/excalidraw/excalidraw
- **リリース情報**: https://github.com/excalidraw/excalidraw/releases
- **最新コミット**: https://github.com/excalidraw/excalidraw/commits
- **Issue・Discussion**: https://github.com/excalidraw/excalidraw/issues
- **プルリクエスト**: https://github.com/excalidraw/excalidraw/pulls

### 公式ドキュメント

- **開発者ドキュメント**: https://docs.excalidraw.com/
- **API 仕様**: https://docs.excalidraw.com/docs/@excalidraw/excalidraw/api
- **統合ガイド**: https://docs.excalidraw.com/docs/@excalidraw/excalidraw/integration
- **開発ガイド**: https://docs.excalidraw.com/docs/introduction/development
- **コントリビューション**: https://docs.excalidraw.com/docs/introduction/contributing
- **JSON Schema**: https://docs.excalidraw.com/docs/codebase/json-schema
- **Utils API**: https://docs.excalidraw.com/docs/@excalidraw/excalidraw/api/utils
- **Export Utilities**: https://docs.excalidraw.com/docs/@excalidraw/excalidraw/api/utils/export

### Excalidraw 関連サービス

- **公式サイト**: https://excalidraw.com/
- **ライブラリ**: https://libraries.excalidraw.com/
- **公式ブログ**: https://blog.excalidraw.com/
- **VS Code 拡張**: https://github.com/excalidraw/excalidraw-vscode

### 技術参考

- **File System Access API**: https://developer.mozilla.org/en-US/docs/Web/API/File_System_Access_API
- **browser-fs-access**: https://github.com/GoogleChromeLabs/browser-fs-access
- **Excalidraw + File System API**: https://blog.excalidraw.com/browser-fs-access/
- **PWA**: https://web.dev/progressive-web-apps/

### Claude Code 実行例

```bash
# 完全セットアップ
claude code "Setup forked Excalidraw with FastAPI backend for corporate use"

# アップデート処理
claude code "Update forked repository from upstream and maintain custom features"

# 機能実装
claude code "Implement folder management API in FastAPI and integrate with Excalidraw frontend"

# テスト・デプロイ
claude code "Test and build production version for corporate deployment"
```

## 実装スケジュール

### フェーズ 1: 基盤構築

1. **リポジトリフォーク・セットアップ**

   - GitHub フォーク をクローン
   - 開発環境構築
   - FastAPI バックエンド基盤

2. **基本 API 実装**
   - ディレクトリ一覧取得
   - ファイル保存・読み込み
   - 基本的なバリデーション

### フェーズ 2: フロントエンド統合

1. **React コンポーネント作成**

   - フォルダ選択 UI
   - ファイル管理インターフェース
   - Excalidraw メニューと U 統合

2. **FastAPI 連携**
   - API クライアント実装
   - エラーハンドリング
   - URL 同期機能

### フェーズ 3: 高度な機能

1. **企業環境対応**

   - ネットワークドライブ対応

2. **UX 改善**
   - UI/UX の洗練
   - パフォーマンス最適化
   - エラーメッセージの改善

### フェーズ 4: テスト・デプロイ（1-2 週間）

1. **包括的テスト**

   - ユニットテスト
   - 統合テスト
   - クロスプラットフォームテスト

2. **プロダクション準備**
   - ビルド最適化
   - 配布パッケージ作成
   - ドキュメント整備

### フェーズ 5: 運用・保守（継続）

1. **アップストリーム追従**

   - 定期的な更新取り込み
   - コンフリクト解決
   - 機能の再適用

2. **機能拡張**
   - ユーザーフィードバック対応
   - 追加機能開発
   - パフォーマンス改善

この包括的な開発ガイドにより、FastAPI バックエンドを使用した Excalidraw フォルダ管理機能の開発から、継続的なアップデート対応まで、効率的に進めることができます。

## 📘 Excalidraw フォルダ管理システム 完全仕様（実装完了）

### ✅ 実装ステータス（2024 年 12 月更新）

- **バックエンド API**: 完了 ✅
- **フロントエンド統合**: 完了 ✅
- **自動保存機能**: 完了 ✅
- **URL パラメータ連動**: 完了 ✅
- **フォルダ管理 UI**: 完了 ✅
- **画像データ保存**: 完了 ✅
- **ブラウザ起動時自動読み込み**: 完了 ✅
- **エンタープライズセキュリティ**: 完了 ✅
- **自動バックアップ機能**: 完了 ✅

## 🚀 実装済み機能詳細

### 1. AutoSaveWrapper（自動保存システム）

**実装場所**:

- `packages/excalidraw/components/AutoSaveWrapper.tsx`
- `packages/excalidraw/hooks/useAutoSave.ts`

**機能**:

- URL パラメータに基づく自動保存（30 秒間隔）
- 2 秒デバウンス機能
- 手動保存（Ctrl+Shift+S）
- 開発モードでのステータス表示
- 画像データ（BinaryFiles）の保存対応

**使用例**:

```tsx
<AutoSaveWrapper
  elements={elements}
  appState={appState}
  files={files}
  onAutoSaveStatusChange={(isEnabled, currentPath) => {
    console.log("自動保存:", isEnabled ? "有効" : "無効", currentPath);
  }}
>
  <Excalidraw />
</AutoSaveWrapper>
```

### 2. ブラウザ起動時自動ファイル読み込み

**実装場所**:

- `excalidraw-app/App.tsx` (`initializeScene`関数)
- `packages/excalidraw/utils/urlParams.ts`

**機能**:

- URL パラメータ（`folder`, `file`）による自動読み込み
- ファイルが存在しない場合の初期画面表示
- エラーハンドリングとフォールバック機能

**URL 例**:

```
# 特定ファイルを開く
http://localhost:3001/?file=my-drawing.excalidraw

# フォルダ内ファイルを開く
http://localhost:3001/?folder=backend&file=my-drawing.excalidraw
```

### 3. FileSystemManager（ファイル管理クラス）

**実装場所**:

- `packages/excalidraw/data/fileSystemManager.ts`
- `packages/excalidraw/data/api-client.ts`

**機能**:

- 統一されたファイル操作 API
- バックエンドとの連携
- エラーハンドリング
- ファイル名の安全化

**主要メソッド**:

```typescript
// ファイル保存
await fileManager.saveToFolder(folderPath, fileName, data);

// ファイル読み込み
const data = await fileManager.loadFromFolder(folderPath, fileName);

// フォルダ構造取得
const structure = await fileManager.getFolderStructure(rootPath);

// ファイル存在確認
const exists = await fileManager.fileExists(folderPath, fileName);
```

### 4. フォルダ管理 UI

**実装場所**:

- `packages/excalidraw/components/FolderSelector.tsx`
- `packages/excalidraw/components/SaveToFolderDialog.tsx`
- `packages/excalidraw/actions/actionFolderSave.tsx`

**機能**:

- フォルダ階層の視覚的表示
- パンくずナビゲーション
- 新しいフォルダ作成
- キーボードナビゲーション（Enter/Space）
- ファイル名の自動補正

### 5. URL パラメータ機能

**実装場所**:

- `packages/excalidraw/utils/urlParams.ts`
- `packages/excalidraw/tests/urlParams.test.ts`

**機能**:

- URL とファイルパスの双方向同期
- パラメータの監視と自動更新
- 安全なファイル名生成

**主要 API**:

```typescript
// URLパラメータ取得
const params = getUrlFileParams();

// URLパラメータ更新
updateUrlFileParams({ folder: "drawings", file: "sketch.excalidraw" });

// ファイルパス生成
const fullPath = getFullFilePath("drawings", "sketch.excalidraw");
```

### 6. 画像データ保存機能

**実装場所**:

- `packages/excalidraw/hooks/useAutoSave.ts`（修正済み）
- `packages/excalidraw/components/App.tsx`

**機能**:

- BinaryFiles（画像データ）の適切な保存
- readonly 配列の mutable 配列変換
- 画像エクスポート機能の改善

**修正内容**:

```typescript
// 修正前: filesプロパティが含まれていなかった
const excalidrawData = {
  type: "excalidraw",
  elements: ...,
  appState: ...
};

// 修正後: 画像データを含める
const excalidrawData = {
  type: "excalidraw",
  elements: ...,
  appState: ...,
  files: files  // 画像データを含める
};
```

### 7. 自動バックアップ機能

**実装場所**:

- `packages/excalidraw/data/backupManager.ts`
- `backend/services/backup_manager.py`
- `packages/excalidraw/data/fileSystemManager.ts`（統合）

**機能**:

- ファイル保存時の自動バックアップ作成
- 設定可能なバックアップ数（デフォルト: 3 個）
- タイムスタンプ付きバックアップファイル名
- 古いバックアップの自動削除
- 手動保存・自動保存での個別設定

**バックアップ設定**:

```typescript
interface BackupConfig {
  maxBackups: number; // 保持するバックアップ数（デフォルト: 3）
  enabled: boolean; // バックアップ機能の有効/無効（デフォルト: true）
  backupOnSave: boolean; // 手動保存時のバックアップ作成（デフォルト: true）
  backupOnAutoSave: boolean; // 自動保存時のバックアップ作成（デフォルト: true）
}
```

**使用例**:

```typescript
// バックアップ設定付きでFileSystemManager初期化
const fileManager = new FileSystemManager("http://localhost:8000", {
  maxBackups: 5,
  enabled: true,
  backupOnSave: true,
  backupOnAutoSave: true,
});

// バックアップ情報の取得
const backupInfo = await fileManager.getBackupInfo(
  "projects",
  "design.excalidraw",
);
console.log(
  `バックアップ数: ${backupInfo.backup_count}/${backupInfo.max_backups}`,
);

// 設定変更
fileManager.updateBackupConfig({
  maxBackups: 10,
  backupOnAutoSave: false,
});
```

**バックアップフォルダ構造**:

```
project-folder/
├── design.excalidraw          # メインファイル
├── diagram.excalidraw         # メインファイル
└── backup/                    # バックアップ専用フォルダ
    ├── design.backup.2024-01-15-14-30-25.excalidraw
    ├── design.backup.2024-01-15-14-25-10.excalidraw
    └── diagram.backup.2024-01-15-14-20-45.excalidraw
```

**バックアップファイル命名規則**:

```
元ファイル名: design.excalidraw
バックアップ: backup/design.backup.2024-01-15-14-30-25.excalidraw
```

**バックアップフロー**:

1. 既存ファイル確認
2. バックアップファイル作成（既存ファイルが存在する場合）
3. メインファイル保存
4. 古いバックアップ削除（設定数を超えた場合）

### 📋 完成ドキュメント

- ✅ **API 仕様書**: バックエンド API 完全仕様
- ✅ **型定義**: TypeScript 型定義（`frontend-api-types.ts`）
- ✅ **API クライアント**: 統合クライアント（`frontend-api-client.ts`）
- ✅ **実装ガイド**: フロントエンド実装手順（`FRONTEND_IMPLEMENTATION_GUIDE.md`）
- ✅ **テストガイド**: ユーザーテスト手順（`USER_TEST_GUIDE.md`）
- ✅ **アーキテクチャ仕様**: 本ドキュメント（`仕様書.md`）

### 🧭 API 一覧まとめ

| メソッド | エンドポイント | 機能 | ステータス |
| --- | --- | --- | --- |
| GET | `/api/v1/files/` | ファイル一覧取得 | ✅ 完了 |
| GET | `/api/v1/files/load/` | ファイル読み込み | ✅ 完了 |
| POST | `/api/v1/files/save/` | ファイル保存 | ✅ 完了 |
| DELETE | `/api/v1/files/` | ファイル削除 | ✅ 完了 |
| POST | `/api/v1/files/directory/` | ディレクトリ作成 | ✅ 完了 |
| GET | `/api/v1/files/info/` | ファイル情報取得 | ✅ 完了 |
| GET | `/health` | ヘルスチェック | ✅ 完了 |
| GET | `/api/v1/files/backup/info/` | バックアップ情報取得 | ✅ 完了 |
| POST | `/api/v1/files/backup/restore/` | バックアップ復元 | ✅ 完了 |
| DELETE | `/api/v1/files/backup/` | バックアップ削除 | ✅ 完了 |

### 🏗️ アーキテクチャ設計

#### base_path と相対パス

- **base_path**: セキュリティ境界（例: `/path/to/excalidraw`）
- **相対パス**: ユーザー指定パス（`base_path`からの相対）
- **絶対パス**: フルパス（セキュリティチェック後のみアクセス可能）

#### セキュリティ機能

- パストラバーサル攻撃防止（`../../../etc/passwd`等）
- ファイル拡張子制限（`.excalidraw`のみ）
- JSON 形式検証
- 危険文字検出（`$`, `` ` ``, `|`, `&`, `;`等）

### 📋 フロントエンド向け API 仕様

#### 1️⃣ ファイル保存 API

**エンドポイント**: `POST /api/v1/files/save/`

**リクエスト**:

```typescript
interface FileSaveRequest {
  file_path: string; // 相対パス（例: "drawings/my_file.excalidraw"）
  content: string; // JSON文字列
  create_directories: boolean; // ディレクトリ自動作成
}
```

**レスポンス**:

```typescript
interface FileSaveResponse {
  file_info: {
    name: string;
    path: string; // 絶対パス
    size: number;
    modified: string; // ISO 8601形式
    is_directory: boolean;
    extension: string;
  };
  success: boolean;
  message: string;
}
```

**使用例**:

```javascript
const response = await fetch("/api/v1/files/save/", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({
    file_path: "drawings/my_drawing.excalidraw",
    content: JSON.stringify(excalidrawData),
    create_directories: true,
  }),
});
```

#### 2️⃣ ファイル読み込み API

**エンドポイント**: `GET /api/v1/files/load/`

**パラメータ**:

```typescript
interface FileLoadParams {
  file_path: string; // 相対パス
}
```

**レスポンス**:

```typescript
interface FileLoadResponse {
  content: string; // JSON文字列
  file_info: FileInfo;
  success: boolean;
}
```

**使用例**:

```javascript
const response = await fetch(
  `/api/v1/files/load/?file_path=${encodeURIComponent(filePath)}`,
);
const data = await response.json();
const excalidrawData = JSON.parse(data.content);
```

#### 3️⃣ ファイル一覧 API

**エンドポイント**: `GET /api/v1/files/`

**パラメータ**:

```typescript
interface FileListParams {
  directory_path?: string; // 相対パス（空の場合はルート）
}
```

**レスポンス**:

```typescript
interface DirectoryListing {
  current_path: string;
  files: FileInfo[];
  total_count: number;
}
```

**使用例**:

```javascript
// ルートディレクトリ
const rootFiles = await fetch("/api/v1/files/").then((r) => r.json());

// サブディレクトリ
const subFiles = await fetch("/api/v1/files/?directory_path=drawings").then(
  (r) => r.json(),
);
```

#### 4️⃣ ファイル削除 API

**エンドポイント**: `DELETE /api/v1/files/`

**パラメータ**:

```typescript
interface FileDeleteParams {
  file_path: string; // 相対パス
}
```

**レスポンス**:

```typescript
interface FileDeleteResponse {
  success: boolean;
  message: string;
}
```

### 🚀 フロントエンド統合ガイド

#### サーバー起動

```bash
cd backend
python main.py
# → http://localhost:8000 で起動
```

#### CORS 設定

- 開発環境: `allow_origins=["*"]`（全オリジン許可）
- 本番環境: 適切なオリジンに制限要

#### エラーハンドリング

```javascript
try {
  const response = await fetch("/api/v1/files/save/", {
    /*...*/
  });

  if (!response.ok) {
    const error = await response.json();
    console.error("API エラー:", error.message);
    // ユーザーに日本語エラーメッセージを表示
  }

  const data = await response.json();
  // 成功処理
} catch (error) {
  console.error("ネットワークエラー:", error);
}
```

#### URL 構築パターン

```javascript
// フォルダ指定機能のためのURL設計
const folderPath = "my-project/drawings";
const fileName = "diagram.excalidraw";
const filePath = `${folderPath}/${fileName}`;

// 保存
await saveFile(filePath, excalidrawData);

// 読み込み
const data = await loadFile(filePath);

// URL同期（将来実装）
// https://excalidraw.com/?folder=my-project/drawings&file=diagram.excalidraw
```
